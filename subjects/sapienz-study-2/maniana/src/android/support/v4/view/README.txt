This package contains a modified ViewPager from the Android SDK. It adds
functionality to control the speed of the programatic horizontal smooth
scroll between pages.  It also required removing the ViewPager* classes
from the library class android-support-v4.jar.